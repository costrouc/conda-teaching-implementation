name: "Documentation"

on:
  pull_request:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2.4.0

      - uses: cachix/install-nix-action@v15
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: "Build and test :gear:"
        run: nix build

      - name: "Deploy 🚀"
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: result
